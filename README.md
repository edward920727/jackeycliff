# 機密代號 - Codenames Online

使用 Next.js 和 Supabase Realtime 打造的線上版《機密代號》遊戲。

## 功能特色

- 🎮 即時多人連線對戰
- 👁️ 隊長視角：可看到所有卡片顏色（使用透明度顯示）
- 👤 隊員視角：點擊卡片後才會顯示顏色
- 🎨 精美的響應式 UI 設計（Tailwind CSS）
- ⚡ 使用 Supabase Realtime 實現即時同步

## 技術棧

- **Next.js 14** - React 框架
- **Supabase** - 後端服務（PostgreSQL + Realtime）
- **TypeScript** - 類型安全
- **Tailwind CSS** - 樣式設計

## 安裝步驟

### 1. 安裝依賴

```bash
npm install
```

### 2. 設置 Supabase

1. 前往 [Supabase](https://supabase.com) 創建新專案
2. 在 Supabase Dashboard 中執行 SQL 遷移：
   - 進入 SQL Editor
   - 執行 `supabase/migrations/001_create_games_table.sql` 中的 SQL

### 3. 配置環境變數

複製 `.env.example` 為 `.env.local` 並填入你的 Supabase 憑證：

```bash
cp .env.example .env.local
```

在 `.env.local` 中填入：
```
NEXT_PUBLIC_SUPABASE_URL=你的_supabase_專案_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的_supabase_anon_key
```

### 4. 啟動開發伺服器

```bash
npm run dev
```

開啟 [http://localhost:3000](http://localhost:3000) 即可開始遊戲！

## 遊戲規則

1. **建立或加入房間**：在首頁選擇角色（隊長或隊員）並建立/加入房間
2. **隊長視角**：可以看到所有卡片的顏色（半透明顯示），但不能點擊
3. **隊員視角**：看不到卡片顏色，需要點擊卡片來翻牌
4. **回合制**：根據點擊的卡片顏色決定是否換回合
5. **勝利條件**：率先翻完所有己方顏色卡片的一方獲勝

## 資料庫結構

### games 表

- `id` (UUID) - 主鍵
- `room_id` (TEXT) - 房間代碼（唯一）
- `words_data` (JSONB) - 卡片數據（詞彙、顏色、是否翻開）
- `current_turn` (TEXT) - 當前回合（'red' 或 'blue'）
- `created_at` (TIMESTAMP) - 創建時間
- `updated_at` (TIMESTAMP) - 更新時間

## 專案結構

```
.
├── app/
│   ├── game/
│   │   └── [roomId]/
│   │       └── page.tsx      # 遊戲主頁面
│   ├── globals.css           # 全域樣式
│   ├── layout.tsx            # 根布局
│   └── page.tsx              # 首頁（房間選擇）
├── lib/
│   └── supabase.ts           # Supabase 客戶端
├── types/
│   └── game.ts               # TypeScript 類型定義
├── supabase/
│   └── migrations/
│       └── 001_create_games_table.sql  # 資料庫遷移
└── package.json
```

## 授權

MIT License
